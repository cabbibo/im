using UnityEditor;
using UnityEngine;

//[InitializeOnLoad]
class EditorMagic:MonoBehaviour
{
/*    public static bool created;
    public static LifeForm God;
    static EditorMagic ()
    {
        EditorApplication.update += Update;
    }

    static void Update ()
    {
        if( EditorApplication.isPaused == false && created == true ){
          God.OnUpdating();
          God.OnDebugging();
//          Debug.Log(EditorApplication.isCompiling);
      }
    }

    public  void Create(){
      Debug.Log("hi");
      God = GameObject.FindWithTag("ENGINE").GetComponent<LifeForm>();
      God.OnCreation();
      created = true;
    }

    public  void Destroy(){
      Debug.Log("destroy");
      God.OnDestruction();
      created = false;
    }*/
}
